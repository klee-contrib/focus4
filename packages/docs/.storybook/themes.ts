import {create} from "storybook/theming";

export const lightTheme = create({
    base: "light",
    brandTitle: "Focus v4",
    fontCode: '"Cascadia Code", Consolas, monospace'
});

export const darkTheme = create({
    base: "dark",
    brandTitle: "Focus v4",
    fontCode: '"Cascadia Code", Consolas, monospace'
});
